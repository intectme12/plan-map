{
  "name": "plan-map",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "postinstall": "npm run prisma:generate --workspace=web",
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down"
  }
}
