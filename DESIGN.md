# plan-map — UI/UX & Design

Phase 1(웹 MVP) ~ Phase 4(비용/사진) 범위의 화면 구조와 디자인 시스템을 정의한다. 모바일 전용 화면(사진첩 자동연동, 만보기)은 Phase 5에서 별도로 다룬다.

## 디자인 원칙

1. **지도가 주인공** — 텍스트 리스트보다 지도 위 마커/경로가 먼저 눈에 들어와야 한다. 좌측 리스트는 지도를 보조한다.
2. **입력을 최소화** — 검색 결과 클릭 한 번으로 장소가 추가되고, AI 파싱 결과는 일괄 승인 한 번으로 반영되어야 한다.
3. **되돌릴 수 있어야 한다** — 삭제/수정은 즉시 반영하되 실행취소(undo) 토스트를 기본 제공한다. 기존 `confirm()`/`alert()` 방식은 폐기.
4. **로딩·에러 상태를 항상 보여준다** — 지금처럼 콘솔 로그로만 실패를 남기지 않고, 스켈레톤/토스트로 사용자에게 노출한다.

## 정보구조 (IA)

```
/                       랜딩 (비로그인 시 로그인 폼 + 서비스 소개)
/trips                  내 여행계획 리스트
/trips/new              여행계획 생성 (제목/일정/인원)
/trips/[tripId]         여행 상세 — 지도 + 일정 타임라인 (핵심 화면)
  ?tab=timeline           장소 타임라인 (기본)
  ?tab=expense            비용 요약
  ?tab=photos             사진 갤러리
/trips/[tripId]/import  AI 텍스트 붙여넣기 → 파싱 결과 확인/승인
```

## 핵심 화면

### 1. 여행 리스트 (`/trips`)
- 카드형 그리드. 카드에 제목, 날짜, 인원, 대표사진(있으면), 총 지출 뱃지.
- 우상단 "새 여행 만들기" 버튼 → 모달로 제목/일정/인원 입력 후 바로 상세 페이지로 이동.

### 2. 여행 상세 (`/trips/[tripId]`) — 데스크탑 2-pane

```
┌─────────────────────┬───────────────────────────────┐
│ 좌: 타임라인 사이드바 │ 우: 지도 (전체 높이)            │
│ - 장소 검색창         │ - 마커 + 순서 번호 배지          │
│ - 장소 카드 리스트      │ - 장소 간 경로 라인(교통수단별 색상)│
│   (드래그로 순서변경)  │ - 카드 클릭 시 해당 마커 포커스+bounce│
│ - 카드별: 시간/교통칩/  │                                 │
│   지출입력/사진썸네일    │                                 │
└─────────────────────┴───────────────────────────────┘
```
- 모바일 웹(반응형)에서는 지도/타임라인을 하단 탭으로 전환(지도 ↔ 리스트), 기존 CRA 버전의 "여행지도리스트/상세여행지도/비용" 탭 구조를 계승하되 3개를 2개+서브탭으로 단순화.
- 장소 카드 사이에 **교통수단 칩**(🚗 자동차 / 🚌 버스) 토글 — 선택한 수단의 소요시간·비용을 카드 사이에 인라인 표시. 기존처럼 클릭해야만 나타나는 게 아니라 기본값(자동차)으로 항상 노출, 클릭 시 버스로 전환.

### 3. 장소 검색
- 사이드바 상단 검색창에 입력 → 결과가 드롭다운 아님, **지도 위에 반투명 오버레이 리스트**로 표시(지도를 가리지 않도록 절반 폭). 항목 클릭 시 지도 마커 미리보기 + "추가" 버튼.

### 4. AI 일정 자동생성 (`/trips/[tripId]/import`)
1. 텍스트 붙여넣기 (placeholder: "블로그 후기나 여행 텍스트를 붙여넣으세요")
2. 파싱 중 스켈레톤 (장소 카드 모양 스켈레톤 3~5개로 "분석 중" 느낌)
3. **확인 화면**: 지도에 후보 마커 전부 표시 + 좌측에 카드 리스트, 카드마다 체크박스(기본 전체 선택) + 모호한 장소는 노란 경고 뱃지("동명 장소 3곳 — 확인 필요")로 구분
4. 하단 "선택한 N개 일정에 추가" 버튼 한 번으로 일괄 반영

### 5. 비용 탭
- 상단에 여행 총액 큰 숫자로 고정 표시, 장소별 지출은 타임라인 카드 안에 인라인 입력(금액만 즉시저장, blur 시 반영).
- 하단에 카테고리별(숙박/식비/교통/기타) 도넛 차트 — 카드 연동 전까지는 장식 아닌 실사용 요약 용도.

### 6. 사진 탭
- 장소별 그룹 그리드. 장소 카드 클릭 → 업로드 모달(드래그앤드롭 + 파일선택). 모바일 자동연동 전까지는 수동 업로드만.

## 디자인 시스템

| 항목 | 값 |
|---|---|
| 폰트 | Pretendard (한글/영문 가독성), 숫자는 tabular-nums |
| 프라이머리 | `#2F6FED` (신뢰감 있는 블루, 지도 마커/버튼) |
| 액센트 | `#FF7A45` (교통/이동 요소 강조, 지출 경고 등과 구분되는 웜톤) |
| 중립 | Tailwind `slate` 스케일 (`slate-50` 배경 ~ `slate-900` 텍스트) |
| 시맨틱 | success `#16A34A`, warning `#D97706`, danger `#DC2626` |
| 반경/그림자 | 카드 radius `12px`, 그림자는 최소화(`shadow-sm`)로 지도와 대비되지 않게 |
| 다크모드 | MVP 범위 아님. 토큰은 CSS 변수로 만들어 Phase 이후 대응 가능하게만 준비 |

**컴포넌트 라이브러리**: Tailwind CSS + shadcn/ui(Radix 기반) 채택. 기존 react-bootstrap은 폐기 — 모달/드롭다운 접근성(포커스 트랩, ESC 닫기)을 기본 제공하는 Radix 프리미티브가 지도 오버레이 UI에 더 적합. 아이콘은 PNG 에셋 대신 `lucide-react`로 통일.

## 핵심 인터랙션 규칙

- **지도 ↔ 리스트 동기화**: 카드 hover 시 마커 살짝 확대, 카드 click 시 지도 `panTo`+포커스. 역방향(마커 클릭 → 카드 스크롤 포커스)도 동일하게 동작해야 함(기존 코드에는 이 양방향 동기화가 없었음).
- **순서 변경**: 카드 드래그 앤 드롭(`@dnd-kit`) 시 저장 API 호출은 debounce 500ms, 드롭 즉시 UI는 optimistic update.
- **삭제**: 즉시 삭제 + 5초 실행취소 토스트. `confirm()` 사용 금지.
- **로딩/에러**: TanStack Query의 `isLoading`/`isError`를 스켈레톤/인라인 에러 배너로 매핑하는 공통 컴포넌트(`<AsyncSection>`) 하나로 통일해 화면마다 제각각 처리하지 않도록 한다.

## 다음 단계

이 문서는 텍스트 기반 초안이다. Phase 1 스캐폴딩 시작 전에 여행 상세 화면(2-pane)만이라도 실제 시안(Figma 또는 코드 목업)으로 한 번 더 확인하고 싶으면 알려달라 — 필요하면 목업 아티팩트로 먼저 그려볼 수 있다.
